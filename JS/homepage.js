// Initialize AOS (Animate On Scroll) Library
AOS.init({
  duration: 1000, // Animation duration in milliseconds (1000ms = 1 second)
  once: true      // Ensures animations occur only once when element scrolls into view (doesn’t repeat on scroll back)
});
